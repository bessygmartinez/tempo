<div class="container overlay-color">
  <div class="row mt-0">
    <div class="col-12 text-center my-0">
      <div class="jumbotron my-0 pt-3 overlay-white">
        <div class="pb-0 mb-0">
        <h1 class="display-3 band-name">DIRECTORY A-Z</h1>
        <hr>
        </div>
        <div class="row mt-4">
        <div class="col-6 mx-auto">
          <table class="table table-striped text-center">
            <thead><span class="display-4">A-E</span></thead>
            <tbody>
              <tr>
                {{#each aebands}}
              <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                  class="text-success">{{this.bandName}}</a></td>
              </tr>
            {{/each}}              
            </tbody>
          </table>
        </div>

        <div class="col-6 mx-auto">
          <table class="table table-striped text-center">
            <thead><span class="display-4">F-J</span></thead>
            <tbody>
              <tr>
                {{#each fjbands}}
              <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                  class="text-success">{{this.bandName}}</a></td>
              </tr>
            {{/each}}              
            </tbody>
          </table>
        </div>
</div>      

<div class="row mt-5">
        <div class="col-6 mx-auto">
          <table class="table table-striped text-center">
            <thead><span class="display-4">K-O</span></thead>
            <tbody>
              <tr>
                {{#each kobands}}
              <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                  class="text-success">{{this.bandName}}</a></td>
              </tr>
            {{/each}}              
            </tbody>
          </table>
        </div>

        <div class="col-6 mx-auto">
          <table class="table table-striped text-center">
            <thead><span class="display-4">P-T</span></thead>
            <tbody>
              {{#each ptbands}}
              <tr>
                
              <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                  class="text-success">{{this.bandName}}</a></td>
              </tr>
            {{/each}}              
            </tbody>
          </table>
        </div>
</div>

<div class="row mt-5">
        <div class="col-6 mx-auto">
          <table class="table table-striped text-center">
            <thead><span class="display-4">U-Z</span></thead>
            <tbody>
              {{#each uzbands}}
              <tr>
                
              <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                  class="text-success">{{this.bandName}}</a></td>
              </tr>
            {{/each}}              
            </tbody>
          </table>
        </div>

        <div class="col-6 mx-auto">
          <table class="table table-striped text-center">
            <thead><span class="display-4"># / !$@*</span></thead>
            <tbody>
              {{#each symbbands}}
              <tr>
                
              <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                  class="text-success">{{this.bandName}}</a></td>
              </tr>
            {{/each}}              
            </tbody>
          </table>
        </div>
</div

</div>



      </div>
    </div>
  </div>

</div>

<script src="/js/index.js"></script>