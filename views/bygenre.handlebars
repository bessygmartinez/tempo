<div class="container max-width-sm overlay-color">
    <div class="row mt-0">
        <div class="col-12 text-center my-0">
            <div class="jumbotron my-0 pt-3 overlay-white">

                <h1 class="display-3 band-name">DIRECTORY BY GENRE</h1>
                <hr>

                <div class="row">
                <div class="col-6 mx-auto mt-4">
                    <table class="table table-striped text-center">
                        <tbody>
                            <thead>
                                        <h4 class="display-4">ELECTRONIC</h4>
                            </thead>
                            <tr>
                                {{#each electronicBands}}
                                <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                                        class="text-success">{{this.bandName}}</a></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>

                <div class="col-6 mx-auto mt-4">
                    <table class="table table-striped text-center">
                        <tbody>
                            <thead>
                                        <h4 class="display-4">HIP-HOP/R&B</h4>
                            </thead>
                            <tr>
                                {{#each hipHopBands}}
                                <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                                        class="text-success">{{this.bandName}}</a></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
</div>

<div class="row mt-4">
                <div class="col-6 mx-auto mt-4">
                    <table class="table table-striped text-center">
                        <tbody>
                            <thead>
                                        <h4 class="display-4">JAZZ</h4>

                            </thead>
                            <tr>
                                {{#each jazzBands}}
                                <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                                        class="text-success">{{this.bandName}}</a></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>

                <div class="col-6 mx-auto mt-4">
                    <table class="table table-striped text-center">
                        <tbody>
                            <thead>
                                        <h4 class="display-4">POP</h4>
                            </thead>
                            <tr>
                                {{#each popBands}}
                                <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                                        class="text-success">{{this.bandName}}</a></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
</div>

<div class="row mt-4">
                <div class="col-6 mx-auto mt-4">
                    <table class="table table-striped text-center">
                        <tbody>
                            <thead>
                                        <h4 class="display-4">ROCK</h4>
                            </thead>
                            <tr>
                                {{#each rockBands}}
                                <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                                        class="text-success">{{this.bandName}}</a></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>

                <div class="col-6 mx-auto mt-4">
                    <table class="table table-striped text-center">
                        <tbody>
                            <thead>
                                        <h4 class="display-4">Other</h4>
                            </thead>
                            <tr>
                                {{#each other}}
                                <td style="font-size: large; font-weight:bold;"><a href="/bands/{{this.bandId}}"
                                        class="text-success">{{this.bandName}}</a></td>
                            </tr>
                            {{/each}}
                        </tbody>
                    </table>
                </div>
</div>

            </div>
        </div>
    </div>

</div>